<div class="users">
	<% for user in @users %>
		<div class="user">
			<%= image_tag user.avatar_url, width: 52, height: 52 %>
		  <%= link_to user.name, user %>
		</div>
  <% end %>
</div>
<%= will_paginate @users %>